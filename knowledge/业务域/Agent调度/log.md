# log

## 2026-07-16

- Claude 引擎主路径改为 `spawnClaudeCodeProcess` + `query` 消息桥：更新 07（pid/`cc_spawn_*`/`CC_LEGACY_QUERY`/stop·watchdog kill）、03（四引擎启动对照）、10（可检索 `cc_spawn_enoent`/`cc_spawn_failed`，非新 ResumeFailureCategory）。

## 2026-07-13

- 同步长任务多会话体验现状：空闲后台预热、同目录通道提示、飞书心跳/入队排队文案、tool 卡住提示落点写入 02/03/06 与跨域飞书/队列篇；领域入口阅读路径补「长任务/多会话体感」。
