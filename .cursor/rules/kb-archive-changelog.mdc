---
description: KB 归档时必须更新 changelog 与 Electron 版本号
alwaysApply: true
---

# KB 归档：Changelog 与版本号

执行 `/kb-archive` 且变更**对用户可见**（功能、修复、体验）时，**必须**同步更新 changelog 并 bump 版本号。纯内部/知识库同步类 lite 变更可跳过。

## 版本号

同步 bump `package.json` 的 `version`（electron-builder 从此读取，无需另改 yml）：

- **patch**（默认）：bug 修复、小优化 → `1.2.1` → `1.2.2`
- **minor**：新功能、行为变更 → `1.2.1` → `1.3.0`
- **major**：破坏性变更 → `2.0.0`

## Changelog 目录结构

所有版本条目存放在 `changelog/`，**按版本一文件**，无根目录索引文件：

```
changelog/
  1.2.2.json
  1.2.1.json
```

单版本文件格式：

```json
{
  "version": "1.2.2",
  "date": "2026-06-27",
  "changes": [
    "用户可理解的变更摘要（中文，一条一个要点）"
  ]
}
```

归档 checklist：

1. bump `package.json` 的 `version`
2. 新建 `changelog/<新版本>.json`
3. 将 `changelog/<版本>.json` 与 `package.json` 写入 `manifest.files`
4. 在 `05-summary.md`「实际变更」中列出上述文件

## 条目写法

- 从 `01-proposal` / `05-summary` 提炼**用户可感知**的变更，勿粘贴 commit 或任务 ID
- 一条 changes 对应一个独立要点；同类可合并，避免空泛词（如单独写「优化」「重构」）

## 关联代码

| 文件 | 作用 |
|---|---|
| `electron/updater.ts` | GitHub API 列举 `changelog/` 或读本地打包目录 |
| `electron-builder.yml` | 打包 `changelog/` |
| `.github/workflows/build.yml` | Release notes 读 `changelog/<version>.json` |
