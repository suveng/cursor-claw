---
description: Cursor Claw SDK 长驻 Agent 通信约定（Legacy 参考模板）
alwaysApply: false
# 仅手动 inject 时启用；launch 不自动注入
---

# ⚠️ 已废弃 / Legacy 模板

> **本文件为历史 CLI/poll 时代参考模板。** SDK-only IM 主路径由 **Daemon dispatch** 投递用户消息，**不依赖**本 rule 注入；`workspace-injector` 已 no-op。**禁止** poll-message；`GET /api/poll-message` 返回 **404**。手动复制至 `.cursor/rules/` 时自行决定 `alwaysApply`。

---

# SDK 长驻 Agent 通信约定

飞书/微信 IM 下 Agent 以 **SDK 长驻 Run** 与 Daemon 协作：入站 dispatch，出站 MCP/HTTP。**无需** Agent 侧 Shell 轮询（保活重连见 `knowledge/业务域/Agent调度/03-启动与自动重连.md`）。

## 入站（Daemon → Agent）

1. Daemon **文件队列** claim → `POST /api/agent/dispatch`（经 Electron **agent-api**）。
2. Agent 从 **Run / Prompt** 接收用户消息，**禁止** Shell 拉取 poll。

会话上下文（`sessionKey`、`messageId`、`chatType`、`senderOpenId`、`botRoster`、`quotedContent` 等）来自 **dispatch / Presentation**，非 HTTP poll。

## 出站（Agent → 用户）

| 方式 | 说明 |
|------|------|
| `send_text` MCP | 主路径；Presentation 负责流式/卡片 |
| `send_image` / `send_file` MCP | 附件 |
| HTTP 降级 | MCP 不可用时直连 `{{DAEMON_PORT}}`（**无 poll 端点**） |

**`send_text`**：`text` 必填；回复时 `message_id` + `session_key` 必填（合并多条取最新 `message_id`）。

| HTTP 降级 | API |
|-----------|-----|
| 文本 | `POST /api/send-text` `{text, message_id?, session_key?}` |
| 图片 | `POST /api/send-image` `{image_path, message_id?, session_key?}` |
| 文件 | `POST /api/send-file` `{file_path, message_id?, session_key?}` |

## 群内 @ 协作（飞书）

- @ 以内联 `@名字(open_id=ou_xxx)` 呈现；身份与 `botRoster` 从 **dispatch/Presentation 上下文** 获取。
- 路由其他机器人：`<at user_id="ou_xxx">名字</at> 任务`（`ou_` 开头 open_id）。
- ⚠️ 防循环：仅必要时 @ 机器人；无需协作时勿回 @；禁止无目的往复 @。

## 状态机（ACK → SYNC → ANCHOR → 等待 dispatch）

1. **ACK**：简单对话直接回复；耗时任务先 `send_text("收到，正在处理…")` 再执行。禁止未 ACK 即长耗时；禁止吞消息。
2. **SYNC**：每完成子步骤 `send_text` 同步进度。禁止长时间无出站。
3. **ANCHOR**：任务完成发送结果摘要后进入等待。禁止未发结果即结束。
4. **等待 dispatch**：Run 长驻，下一条由 Daemon dispatch 投递；**禁止** Shell poll 循环。

**workflow**：按 Prompt 节点执行；咨询用户用 `send_text`；完成 `workflow_next`，失败 `workflow_reject`。

## 消息回复规则

`send_text` / `send_image` / `send_file` 须带 `message_id` + `session_key`：每条用户消息必须回复；合并回复取最新 `message_id`；`messageId` 为空时沿用记忆中的 id。

## 核心禁令

1. 禁止剧终幻觉（「再见」等结束语）。
2. 禁止直接输出——结论须经 `send_text`。
3. 禁止 poll-message（`GET /api/poll-message` 已 404）。
4. 每条用户消息须有出站回复。

## 异常熔断

工具异常时最多静默重试 3 次；连续失败则输出 `⚠️ [消息通道连接异常] 请检查本地服务或网络后重启应用重试。`

## 已移除（历史 CLI 协议）

CLI spawn 时代的 poll-message 五阶段状态机、Shell 保活轮询、SYSTEM OVERRIDE 与阻塞 poll 已移除；长驻由 Daemon 负责。详见归档 `20260627162620-飞书作为Cursor展示与控制层`。
